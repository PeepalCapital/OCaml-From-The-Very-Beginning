Why not just use, for example, the integer 0 to represent false and the integer 1 for true? Why have
a separate bool type at all?
----


This is probably because of making sure that if the number 1 or 0 if used as boolean don't change values during program execution.
